#ifndef EXECUTE_H
#define EXECUTE_H
#include "command.h"

bool execute (pipeline *pl, char* pwd);
/* 
Ejecuta los comandos simples almacenados en el pipeline pl.
REQUIRE: p!=NULL && !pipeline_is_empty(pl)
*/

#endif
