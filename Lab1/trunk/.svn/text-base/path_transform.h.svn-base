#ifndef PATH_TRANSFORM_H
#define PATH_TRANSFORM_H
#define _GNU_SOURCE
#include "command.h"

bool path_transform (pipeline * pl, char* pwd);
/*
 * chequea si el scommand existe el el filesystem y le concatena   
 * la direccion correspondiente obtenida en caso de no encontrarse  
 * definida en scommand una dirección relativa o absoluta.
 * Returns : True si se encuentra el path , en caso de error False.
 *			 Ademas en caso de error , se imprimira un mensaje por
 *			 la salida de errores.
 * Requires :  pl != NULL 
 */
 
#endif
